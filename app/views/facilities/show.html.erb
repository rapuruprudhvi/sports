<!DOCTYPE html>
<html>
<head>
  <title>Facility Details</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Additional Custom Styles */
    /* You can add your custom styles here */
    .facility-details {
      margin-bottom: 20px;
    }
    .booking-list {
      list-style-type: none;
      padding-left: 0;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <p class="alert alert-success"><%= notice %></p>

    <%= render @facility %>

    <div class="mb-3">
      <%= link_to "Edit this facility", edit_facility_path(@facility), class: "btn btn-primary mr-2" %>
      <%= link_to "Back to facilities", facilities_path, class: "btn btn-secondary mr-2" %>
      <%= button_to "Destroy this facility", @facility, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
    </div>

    <h1 class="mb-3"><%= @facility.name %></h1>
    <p><strong>Description:</strong> <%= @facility.description %></p>
    <p><strong>Location:</strong> <%= @facility.location %></p>
    <p><strong>Sport:</strong> <%= @facility.sport %></p>
    <p><strong>Pincode:</strong> <%= @facility.pincode %></p>

    <h2 class="mt-4 mb-3">Bookings</h2>
    <ul class="booking-list">
      <% @bookings.each do |booking| %>
        <li class="mb-2"><%= booking.start_time %> to <%= booking.end_time %></li>
      <% end %>
    </ul>

    <%= link_to 'New Booking', new_facility_booking_path(@facility), class: "btn btn-success mr-2" %>
  </div>

  <!-- Bootstrap JS (optional) -->
  <!-- jQuery is required for Bootstrap's JavaScript plugins -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
