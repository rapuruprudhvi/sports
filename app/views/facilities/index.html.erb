<!DOCTYPE html>
<html>
<head>
  <title>Facilities</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
  </style>
</head>
<body>
  <div class="container mt-4">
    <p class="alert alert-success"><%= notice %></p>

    <h1> Welcome to our Facilities</h1>

    <div class="row" id="facilities">
      <% @facilities.each do |facility| %>
        <div class="col-md-4 mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= facility.name %></h5>
              <p class="card-text"><%= facility.description %></p>
              <p class="card-text"><strong>Location:</strong> <%= facility.location %></p>
              <p class="card-text"><strong>Sport:</strong> <%= facility.sport %></p>
              <p class="card-text"><strong>Pincode:</strong> <%= facility.pincode %></p>
              <%= link_to "Show this facility", facility, class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
     
    <%= link_to "New facility", new_facility_path, class: "btn btn-success mt-3" %>


    <h1 class="mt-5">Sports Facilities</h1>

    <%= form_with url: facilities_path, method: :get, local: true do |form| %>

      <div class="form-group">
        <%= form.label :pincode %>
        <%= form.text_field :pincode, class: "form-control" %>
      </div>

      <%= form.submit 'Filter', class: "btn btn-primary" %>
    <% end %>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
